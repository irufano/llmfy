[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "llmfy"
dynamic = ["version"]
description = "`LLMfy` is a framework for developing applications with large language models (LLMs)."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "irufano", email = "irufano.official@gmail.com" },
]
keywords = [
    "llm",
    "ai",
    "llm-framework",
    "llm-abstraction",
    "bedrock",
    "openai",
    "google",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "pydantic",
    "spacy>=3.8.14",
    "xx-ent-pii-sm",
]

[project.urls]
Homepage = "https://github.com/irufano/llmfy"
Repository = "https://github.com/irufano/llmfy"

[project.optional-dependencies]
openai = ["openai"]
boto3 = ["boto3"]
google-genai = ["google-genai"]
numpy = ["numpy"]
faiss-cpu = ["faiss-cpu"]
typing_extensions = ["typing_extensions"]
redis = ["redis"]
SQLAlchemy = ["SQLAlchemy"]
spacy = ["spacy"]
all = [
    "openai",
    "boto3",
    "google-genai",
    "numpy",
    "faiss-cpu",
    "typing_extensions",
    "redis",
    "SQLAlchemy",
    "spacy",
]

[dependency-groups]
dev = [
    "python-dotenv>=1.1.0",
    "openai>=1.81.0",
    "boto3>=1.38.21",
    "google-genai>=1.69.0",
    "numpy>=2.3.3",
    "faiss-cpu>=1.12.0",
    "redis>=7.1.0",
    "SQLAlchemy>=2.0.44",
    "typing_extensions>=4.15.0",
    "PyMySQL>=1.1.2",
    "spacy>=3.8.0",
]
docs = [
    "mkdocs>=1.6.1",
    "mkdocs-get-deps>=0.2.0",
    "mkdocstrings>=0.30.1",
    "mkdocstrings-python>=1.18.2",
    "mkdocs-material>=9.6.20",
    "mkdocs-material-extensions>=1.3.1",
    "mkdocs-material[imaging]",
    "watchdog>=6.0.0",
]

[tool.hatch.version]
source = "vcs"
tag-pattern = "v(?P<version>.*)"

[tool.hatch.build.hooks.vcs]
version-file = "llmfy/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["llmfy"]
exclude = ["llmfy/test/"]

[tool.hatch.build.targets.sdist]
include = [
    "llmfy/",
    "README.md",
    "LICENSE",
    "pyproject.toml",
]
exclude = ["llmfy/test/"]

[tool.uv.sources]
xx-ent-pii-sm = { url = "https://github.com/irufano/spacy_ner_pii/releases/download/v0.1.0/xx_ent_pii_sm-0.1.0-py3-none-any.whl" }

[tool.ruff]
target-version = "py311"
exclude = ["llmfy/_version.py"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B"]

[tool.ruff.lint.isort]
known-first-party = ["llmfy"]
